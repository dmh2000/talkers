# Makefile for internal/errors

.PHONY: all lint test build clean

all: clean lint build

lint:
	@echo "Running golangci-lint on internal/errors..."
	@golangci-lint run .

test:
	@echo "No tests in internal/errors directory"

build:
	@echo "No build required for internal/errors (library package)"

clean:
	@echo "No artifacts to clean in internal/errors"
