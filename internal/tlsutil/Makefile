# Makefile for internal/tlsutil

.PHONY: all lint test build clean

all: clean lint build

lint:
	@echo "Running golangci-lint on internal/tlsutil..."
	@golangci-lint run .

test:
	@echo "No tests in internal/tlsutil directory"

build:
	@echo "No build required for internal/tlsutil (library package)"

clean:
	@echo "No artifacts to clean in internal/tlsutil"
